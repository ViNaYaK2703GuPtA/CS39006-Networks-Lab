Run the program by "make"
Run the server by "sudo ./server"
Run the client by "sudo ./client <mac address of your machine>"

for finding mac address of the machine use "ifconfig" and ue the field in ether
